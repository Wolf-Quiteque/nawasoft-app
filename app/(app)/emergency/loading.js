import PageSkeleton from '@/components/ui/PageSkeleton';

export default function Loading() {
  return <PageSkeleton title="Emergência" subtitle="Parar ou retomar vendas em todo o sistema" count={3} />;
}
